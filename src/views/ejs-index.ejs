<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>
    <%= title %>
  </title>
</head>

<body>
  <h1>
    <%= heading %>
  </h1>
  <p>
    <%= message %>
  </p>

  <blockquote>
    <% items.forEach((item, i) => {
      i++ %>
      <li>
        <%= i %> айтем: <%= item %>
      </li>
      <% }); %>
  </blockquote>

  <%- include('footer') %>
</body>

</html>