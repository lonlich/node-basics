/*
==================================================
                   Prisma Cheatsheet
==================================================

📌 Модели (models)
-------------------
model User {
  id    Int     @id @default(autoincrement())   // Primary Key с автоинкрементом
  email String  @unique                          // Уникальное поле
  name  String?                                 // Nullable поле
  posts Post[]                                  // Связь один-ко-многим
}

📌 Атрибуты полей (Field Attributes)
-------------------------------------
@id                      // Primary Key
@default(autoincrement()) // Автоинкремент (числа)
@default(uuid())          // UUID по умолчанию
@default(now())           // Текущая дата/время
@unique                  // Уникальное поле
@updatedAt               // Обновляется при изменении записи
@db.VarChar(255)         // Указание типа колонки в БД

📌 Связи и каскады (Relations)
-------------------------------
relation fields: [authorId], references: [id]
onDelete / onUpdate:
- Cascade  — удаление родителя удаляет дочерние записи
- SetNull  — при удалении родителя ставит NULL в дочерних
- Restrict — не даёт удалить родителя, если есть дети
- NoAction — игнорировать (ФК всё равно работает)

Пример:
author User @relation(fields: [authorId], references: [id], onDelete: Cascade, onUpdate: NoAction)

📌 Команды миграций (migrations)
-------------------------------
npx prisma migrate dev --name <имя>  — создать миграцию и применить
npx prisma migrate reset              — сбросить базу (данные удалятся!)
npx prisma migrate status             — проверить статус миграций

📌 Работа с существующей БД
---------------------------
npx prisma db pull     — импортировать структуру из БД в schema.prisma
npx prisma db push     — применить изменения из schema.prisma в БД без миграций
npx prisma generate    — сгенерировать Prisma Client после изменений

📌 Prisma Client (JS/TS) — базовые операции
-------------------------------------------
await prisma.user.findMany()                      // SELECT *
await prisma.user.create({ data: { email: 'a@a' } })  // INSERT
await prisma.user.update({ where: { id: 1 }, data: { name: 'New' } }) // UPDATE
await prisma.user.delete({ where: { id: 1 } })     // DELETE

==================================================
*/


